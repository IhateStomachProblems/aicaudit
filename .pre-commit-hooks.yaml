# pre-commit hook definitions for AICAudit.
# Local usage until the package is on PyPI:
#   repos:
#     - repo: local
#       hooks:
#         - id: aicaudit
#           name: aicaudit
#           entry: aicaudit scan --fail-on error
#           language: system
#           types: [python]
- id: aicaudit
  name: aicaudit
  description: Evidence-driven AI code audit for Python (taint paths + AI verdicts)
  entry: aicaudit scan
  language: python
  types: [python]
